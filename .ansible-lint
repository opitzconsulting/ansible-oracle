---

use_default_rules: true

skip_list:

  - var-naming
  # temporary deactived - will be removed in next PRs
  - fqcn-builtins
  - schema

exclude_paths:
  - .github
  - dbhome-conversion
  - plugins/modules
  - playbooks
